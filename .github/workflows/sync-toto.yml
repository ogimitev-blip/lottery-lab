name: Sync official Toto draws

on:
  workflow_dispatch:
  schedule:
    - cron: "30 20 * * 4"
    - cron: "30 20 * * 0"
    - cron: "30 22 * * 4"
    - cron: "30 22 * * 0"

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pandas requests beautifulsoup4
      - name: Sync newest official result
        run: python scripts/sync_toto.py
      - name: One-time 2026 dates and payout backfill
        run: python scripts/backfill_2026.py
      - name: Commit if changed
        run: |
          if git diff --quiet && [ -z "$(git status --porcelain)" ]; then
            echo "No new official data found."
            exit 0
          fi
          git config user.name "lottery-lab-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/
          git commit -m "Sync official Toto data"
          git push
